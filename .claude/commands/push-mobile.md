# Push to Mobile

Push latest Mac changes to Obsidian mobile folder (run BEFORE using iPhone).

Execute the push-to-mobile script:

```bash
bash scripts/push-to-mobile.sh
```

After running, inform the user:
- Wait 1-2 minutes for iCloud sync
- Then they can open Obsidian on iPhone with latest changes
- Run /pull-mobile when done on iPhone
